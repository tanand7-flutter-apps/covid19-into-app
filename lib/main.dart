import 'package:flutter/material.dart';

void main() {
  runApp(const CovidInfoApp());
}

class CovidInfoApp extends StatelessWidget {
  const CovidInfoApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
